
class Annotatable {
    /**
     * @param featureKey
     */
    set featureKey(featureKey) {
        this._featureKey = featureKey;
    }

    get featureKey() {
        return this._featureKey;
    }
}